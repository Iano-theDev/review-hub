import { DescShortenerPipe } from './desc-shortener.pipe';

describe('DescShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
